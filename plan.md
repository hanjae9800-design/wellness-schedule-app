# 작업 계획: 파일 업로드 → AI가 읽고 자동으로 추진일정 생성

## 요청 내용
추진일정 사이트에 본인 드라이브의 파일을 끌어다 놓으면(드래그앤드롭), 그 파일 내용을 AI가 읽고 분석해서 자동으로 프로젝트 추진일정(구분/업무/일정)을 만들어주는 기능.

## 확정된 흐름
1. 프로젝트 화면(편집 모드)에서, **어느 위치든 상관없이** 파일을 브라우저 창 위로 끌고 오면 화면 전체에 "파일을 놓으면 자동으로 일정이 생성됩니다" 오버레이가 뜸
2. 파일을 놓으면(drop) → **"기존 업무를 덮어쓸까요, 새로 추가할까요?"** 팝업으로 물어봄
3. 브라우저가 파일 + 선택(덮어쓰기/추가)을 서버(Cloudflare Pages Function, 새로 추가)로 전송
4. 서버가 파일 내용을 AI에 보내서 "구분(phase) / 업무명 / 시작일 / 종료일 / 담당자" 형태의 구조화된 목록으로 뽑아달라고 요청
5. 서버가 결과를 브라우저로 돌려주면, **미리보기 없이 바로** 선택한 방식(덮어쓰기/추가)대로 Supabase에 반영
6. 잘못 뽑힌 내용은 이미 있는 업무 목록 편집 화면에서 담당자가 직접 고치는 방식 (완벽한 추출을 목표로 하지 않음)

## 왜 서버(Function)가 필요한지
지금 이 사이트는 브라우저에서 Supabase에 직접 붙는 구조라 별도 서버가 거의 없는데(관리자 비밀번호 확인용 함수 하나만 있음), AI 호출은 API 키가 필요해서 브라우저 코드에 그대로 넣으면 키가 공개로 노출됩니다. 그래서 관리자 페이지처럼 Cloudflare Pages Function을 하나 새로 만들어서, 그 안에서만 AI API를 호출하게 할 계획입니다. (API 키는 Cloudflare 환경변수(시크릿)로 저장 — 관리자 비밀번호와 같은 방식)

## 파일 형식
우선 **텍스트 계열(txt, md)과 PDF**만 지원. 워드(docx)·엑셀(xlsx)은 변환 라이브러리가 추가로 필요해서 이번엔 빼고, **이후 별도 작업으로 추가 예정** (지금 단계에서 미리 챙겨야 할 할 일로 기록).

## AI를 무료로 쓰는 방법 — 조사한 옵션 3가지

### 옵션 1: Google Gemini API 무료 티어 (추천)
- Gemini는 무료 사용량 한도 안에서는 정말 비용이 없습니다. 신용카드 등록 없이 Google AI Studio에서 키 발급 가능.
- PDF를 모델이 직접 읽을 수 있어서 지금 설계(파일을 그대로 AI에 전달)와 잘 맞습니다.
- 단점: Claude가 아니라 구글 모델을 쓰게 됨. 무료 한도라 사용량이 갑자기 몰리면(예: 짧은 시간에 여러 번 업로드) 요청이 잠깐 실패할 수 있음 — 실무 규모에서는 크게 문제되지 않을 정도의 한도입니다.

### 옵션 2: Cloudflare Workers AI 무료 사용량
- 이미 Cloudflare를 쓰고 있어서 별도 가입 없이 바로 사용 가능, 하루 무료 사용량 안에서는 비용 없음.
- 단점: 여기 모델들은 PDF를 직접 못 읽어서, PDF는 텍스트 추출하는 단계를 하나 더 만들어야 함 (txt/md는 바로 가능). 구현이 옵션 1보다 조금 더 복잡해집니다.

### 옵션 3: Anthropic API 신규 가입 크레딧
- Claude를 그대로 쓸 수 있고 문서 이해 품질이 제일 좋습니다.
- 단점: 가입 시 1회성 소액 크레딧(보통 몇 달러 수준)이 소진되면 그 뒤로는 결제 수단 등록이 필요합니다 — "완전 무료"라기보다 "당분간 무료"에 가깝습니다.

**어느 옵션으로 진행할까요?** (옵션 1을 추천드리지만, Claude 품질을 우선하신다면 옵션 3도 소액으로 시작하기엔 괜찮습니다)

---
## 확정 — 구현 진행합니다
- AI: Google Gemini API 무료 티어
- 파일 형식: txt / md / PDF (워드·엑셀은 추후 별도 작업)
- 미리보기 없이 바로 반영, 업로드 시 덮어쓰기/추가 팝업으로 선택
- 화면 아무 곳에나 드래그하면 전체 오버레이 표시

**남은 건 한재님이 직접 해주셔야 하는 부분**: Google AI Studio(aistudio.google.com)에서 무료 Gemini API 키 발급 → 발급 후 `npx wrangler pages secret put GEMINI_API_KEY` 명령을 직접 터미널에서 실행해서 키를 등록해주세요 (키 값은 저한테 붙여넣지 않으셔도 됩니다 — 명령 실행하면 터미널이 따로 물어봅니다). 코드는 지금 바로 구현하겠습니다.