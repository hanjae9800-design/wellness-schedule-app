# 업무 목록 · 간트차트 바깥 테두리 박스 (1cm 간격)

## 확정 (한재님 코멘트 반영, 최종)
- 이번 작업 범위는 **간트차트(`.gantt-wrap`) · 업무 목록 표(`.table-scroll`) 딱 두 패널만**.
- 두 패널 자체(내부 배경·padding·sticky 열 등)는 **전혀 건드리지 않음**.
- 그 바깥에 **새 테두리 박스**를 하나 더 씌워서, 패널 바깥쪽 테두리와 새 박스 테두리 사이 간격이 1cm(≈38px)가 되도록 함.
- 헤더(`.page-head`)·통계 칩(`.stats`)·체크리스트 메모박스·담당자별 현황 박스 등 **다른 패널은 이번엔 손 안 댐**.
- **데스크톱 화면에만 적용, 모바일은 나중에 별도로.** 간트차트/업무 목록 표는 이미 `<section class="board desktop-only">` 안에 있어서, 이 두 요소만 감싸면 자동으로 데스크톱에만 적용됨 — 모바일 카드 화면·간트 오버레이는 원천적으로 영향 없음.

## 원래 요청 내용 (참고)
- lol.ps 사이트처럼 페이지 상단 내용을 위로 정리 + 각 패널 뒤에 박스 테두리 → 위 "확정" 항목처럼 간트차트·업무 목록 두 패널로 범위 축소됨. 헤더 쪽("위로 정리")은 이번 범위에서 빠짐.

## 구체적 구현 방법

1. `style.css`에 새 클래스 추가:
   ```css
   .panel-frame { background: var(--surface-alt); border: 1px solid var(--border-strong); border-radius: 18px; padding: 38px; box-sizing: border-box; }
   ```
   **[코멘트 반영]** 배경은 투명이 아니라 `var(--surface-alt)`로 채움 — 지금 팔레트에서 페이지 배경(`--page`)과 카드 배경(`--surface`) 사이 톤인 색이라, 뒤쪽 페이지 배경과는 구분되면서도 전체 팔레트 톤은 그대로 유지됨. 결과적으로 바깥→안쪽 순서로 `--page`(페이지) → `--surface-alt`(새 테두리 박스) → `--surface`(기존 간트/표 카드) 3단 톤이 생김.

   
2. `index.html`에서 두 군데를 감싸는 wrapper `<div class="panel-frame">`를 추가:
   - `#ganttViewGroup` 안의 `<div class="gantt-wrap" id="ganttWrap">...</div>` 전체를 감쌈
   - `#tableViewGroup` 안의 `<div class="table-scroll">...</div>` 전체를 감쌈
   - `id="ganttWrap"` 등 기존 id는 그대로 유지 (JS에서 `#ganttWrap` 기준으로 스크롤 동기화하는 코드가 많아서 id·구조는 안 건드림 — 바깥에 한 겹만 새로 씌우는 것)

   
3. 기존 `.gantt-wrap`/`.table-scroll`에 걸려 있던 `margin-bottom` 값은 새 `.panel-frame` 쪽으로 옮겨서, 두 섹션(간트차트 / 업무 목록) 사이 간격이 지금과 비슷하게 유지되도록 조정.
4. 데스크톱 전용이라 별도 미디어쿼리 처리 불필요 (이미 `desktop-only`가 부모에 걸려 있음).

## 작업 범위 밖
- 색상/폰트/기존 컴포넌트 디자인 변경 없음.
- 통계 칩·체크리스트 메모박스·담당자별 현황 박스 — 이번엔 손 안 댐 (필요하시면 다음에 별도로 요청해주세요).
- 모바일 카드 화면(`mobile-only`), 모바일 간트 오버레이 — 이번엔 손 안 댐.

---

## 추가 요청: 헤더 박스 (2차, 완료)

- "헤더 박스도 진행해줘" → `.page-head`를 `.panel-frame`으로 감쌈. **배포 완료.**

## 추가 요청: 헤더를 lol.ps 최상단 바 스타일로 재설계 (3차, 진행 예정)

- "헤더를 저렇게 하지말고 페이지 상단으로 한줄로 정리되게끔 롤.ps 사이트 최상단 헤더 스타일처럼 해줘" → 2차에서 만든 카드형 박스 헤더는 **되돌리고**, 아래처럼 새로 만듭니다.
- lol.ps 실제로 스크롤해서 확인해본 결과: 상단 바가 **화면 가장자리까지 꽉 찬 폭(centered 카드 아님) + 스크롤해도 항상 맨 위에 고정(sticky) + 둥근 모서리 없는 한 줄짜리 바** 였습니다. 이 3가지가 lol.ps 헤더의 핵심 특징이라 그대로 반영하려고 합니다.

### 제안하는 구조 (한 줄로 정리)
```
[☰]  ← 목록  |  사업명 / 부서 / PM  [팀칩들] +팀원추가  |  프로젝트명 추진일정      [지연] [예정▾] [편집 가능] [보기 전용으로 잠그기]  [🌙]
```
- 지금 좌상단에 따로 떠 있던 `☰`(뷰 전환) 버튼과 우상단에 따로 떠 있던 `🌙`(다크모드) 버튼도 **이 바 안으로 합칩니다.** (lol.ps도 별도로 뜬 아이콘 없이 전부 상단 바 하나에 들어있는 구조라서요). 지금처럼 화면에 각자 따로 떠 있는 원형 버튼 2개 + 새 상단 바, 이렇게 이중으로 떠 있는 것보다 한 군데로 합치는 게 lol.ps 느낌에 더 가깝고 깔끔합니다.
- 배경색은 `var(--surface)` (지금 카드들과 같은 톤) + 아래쪽 테두리선 + 그림자로 페이지 배경과 구분. (진한 강조색으로 완전히 다른 톤을 쓰는 것도 가능하지만, 그러면 배지·버튼 색상들을 전부 새로 맞춰야 해서 이번엔 톤만 다르게 구분되는 쪽으로 진행하려고 합니다.)
- 화면 폭이 좁아서 한 줄에 다 안 들어가는 극단적인 경우엔 레이아웃이 깨지는 것보다는 그 바 안에서만 가로 스크롤되게 안전장치를 둡니다.
- 지금 `.app`(가운데 정렬, 최대폭 1280px) 안에 있던 헤더를 그 바깥, `<body>` 최상단으로 옮겨야 진짜 "가장자리까지 꽉 찬 폭"이 나옵니다 — 코드 위치상 구조 변경이 필요합니다. 대신 id는 그대로 유지해서 자바스크립트 동작에는 영향 없습니다.
- 프로젝트 상세 화면에서만 보이는 요소라, 목록(landing) 화면에는 안 나타나게 그대로 유지합니다.

이대로 진행해도 될까요? (다른 의견 없으시면 바로 진행하겠습니다)

## 완료 — 실제 구현 결과
- 2차에서 만든 카드형 헤더(`.panel-frame`으로 감싼 헤더)는 되돌림.
- 새 `<div class="top-bar" id="topBar">`를 `<body>` 최상단(`#app`, `#landing` 바깥)에 추가 — `position:sticky; top:0`, 폭 100%, 배경 `var(--surface)` + 아래쪽 테두리선 + 그림자. 프로젝트 상세 화면에 들어갈 때(`enterProject()`)만 `hidden`을 풀어서 보여줌 (목록 화면에는 안 나타남).
- 좌상단에 따로 떠 있던 `☰`(뷰 전환) 버튼을 이 바 안으로 이동. 우상단 `🌙`(다크모드) 버튼은 목록 화면에서도 공용으로 쓰이는 요소라 그대로 고정 위치 유지하되, 바 오른쪽에 여백(60px)을 둬서 안 겹치게 처리.
- 한 줄 레이아웃: `☰ | ← 목록 | 사업명/부서/PM+팀원칩 | 프로젝트명 추진일정 | (spacer) | 상태배지 | 상태선택 | 편집모드배지 | 편집/잠그기 버튼`
- 폭이 좁아지면 가운데 "사업명/부서/PM" 구분 영역이 먼저 줄어들어 잘리고(overflow:hidden), 오른쪽 상태·편집모드 버튼은 항상 보이도록 보호 — 처음엔 이 부분에서 오른쪽 버튼들이 화면 밖으로 밀려나가 안 보이는 문제가 있었는데, 실제 스크린샷으로 확인하면서 잡음.
- **모바일(880px 이하)**: 요청하신 대로 이번 lol.ps 스타일(가장자리까지 꽉 참·고정·한 줄)은 적용 안 함. 대신 같은 마크업을 미디어쿼리로 다시 스타일링해서 기존처럼 자연스럽게 줄바꿈되는 카드형으로 복원 — 뒤로가기·제목·상태·편집모드 등 기능은 모바일에서도 그대로 다 보임 (사라진 기능 없음). ☰ 버튼은 원래도 모바일엔 없던 기능이라 그대로 숨김.
- 라이트/다크 모드, 데스크톱 스크롤(sticky 확인), 모바일 3가지 다 Playwright로 스크린샷 검증 완료 후 배포·커밋·푸시.

## 추가 요청: 간트차트·업무 목록 테두리 박스 제거 (4차, 완료)
- "업무목록, 간트차트 패널에 박스 테두리 그냥 없애줘" → 1차에서 만든 `.panel-frame` 바깥 테두리 박스를 완전히 되돌림. `.gantt-wrap`/`.table-scroll`은 원래 자기 자신의 카드 스타일(배경·테두리·그림자)만 남고, 그 바깥을 감쌌던 여분의 테두리 박스는 삭제. 스크린샷 확인 후 배포·커밋·푸시.
- (참고: 위 1차 섹션에 "바 형태 말고 직사각형 박스로" 코멘트가 남아있는데, 4차에서 이 테두리 박스 자체를 통째로 없앴기 때문에 이제 해당 없는 코멘트입니다 — 혹시 다시 테두리 박스를 원하시면 그때 반영하겠습니다.)

## 추가 요청: 체크리스트 헤더 정리 + 라이트/다크 버튼 중앙 배치 (5차, 완료)
- "헤더에 '업무 체크리스트' 문구를 좌측으로 옮기고 사이사이에 일자로 선 있는거 없애줘. 그리고 지연, 예정 칸 없애주고 라이트/다크 모드 버튼 헤더 중간 위치로 정리해줘"
- **해석/가정**: "업무 체크리스트" 문구를 언급한 요청이라, 지연·예정 칸 제거와 구분선 정리는 **체크리스트 타입 프로젝트에만** 적용했습니다 (추진일정 타입은 프로젝트 상태 추적이 의미가 있어서 그대로 둠). 라이트/다크 버튼 중앙 배치는 헤더 자체의 레이아웃 문제라 **두 타입 모두**에 적용했습니다. 이 가정이 틀렸으면 말씀해주세요.
- 원인 파악: 체크리스트 프로젝트는 원래도 사업명/부서/PM/팀원 입력칸이 전부 숨겨져 있었는데(개인용이라 불필요), 그 자리를 감싸던 "|" 구분선 2개는 안 지워져서 빈 칸 사이에 선만 덩그러니 남아있던 게 문제였음.
- 구현:
  - `tb-crumb`(사업명/부서/PM/팀원)와 그 앞뒤 "|" 구분선을 `#tbCrumbGroup`이라는 하나의 묶음으로 감싸고, 체크리스트 타입이면 이 묶음 전체를 숨김 → "← 목록 | 업무 체크리스트"처럼 제목이 바로 뒤따라와서 자연스럽게 왼쪽으로 붙음 (순서를 바꿀 필요 없음 — 체크리스트는 원래 제목 칸에 "업무 체크리스트" 고정 라벨 하나만 보이는 구조라서).
  - `statusBadge`(지연 등)·`statusSelect`(예정/진행중/완료)도 체크리스트 타입이면 숨김.
  - 헤더를 좌(뒤로가기·구분·제목) / 중앙(라이트·다크) / 우(상태·편집모드) 3분할 구조로 재구성 — 좌우를 동일한 flex 비율로 둬서 라이트/다크 버튼이 콘텐츠 길이와 상관없이 항상 바 정중앙에 오도록 함.
  - 원래 화면 우상단에 고정으로 떠 있던 다크모드 버튼은 프로젝트 상세 화면에서는 숨기고(목록 화면에서는 그대로 유지 — 공용 버튼이라), 상세 화면에서는 헤더 중앙에 새로 넣은 버튼이 그 역할을 대신하도록 처리.
  - 실제 체크리스트 타입 프로젝트가 DB에 없어서(있어도 RLS로 익명 조회 불가), Playwright로 추진일정 프로젝트를 열어 `applyChecklistModeUI()`가 하는 것과 똑같이 DOM을 직접 조작해 체크리스트 헤더 모습을 재현해서 검증함.
  - 라이트/다크/모바일 스크린샷 확인 후 배포·커밋·푸시.

## 추가 요청: 라이트/다크 버튼 위치 재조정 (6차, 완료)
- "라이트/다크 모드 버튼 맨 왼쪽으로 옮겨줘" → 맨 왼쪽으로 옮겼다가, 바로 이어서 "미안 맨 왼쪽이 아닌 맨 오른쪽으로 위치해줘"로 정정 → 최종적으로 헤더 맨 오른쪽(잠그기/편집모드 버튼 뒤)으로 배치. 5차에서 만들었던 좌/중앙/우 3분할 구조는 더 이상 안 써서 좌/우 2분할로 단순화. 스크린샷 확인 후 배포·커밋·푸시.
