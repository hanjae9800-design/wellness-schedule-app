# 작업 계획: 배포 과정 시간 단축

## 1. plan.md / .claude 제외 배포를 한 번에

### 확인한 사실
`.assetsignore` 파일로 제외되게 해봤는데 방금 실제로 다시 테스트해봐도 **여전히 안 먹힙니다** (18개여야 할 파일이 24개로 전부 올라감, `plan.md`가 실제로 공개 배포됨을 재확인 → 바로 원상복구 배포까지 완료했습니다). wrangler 자체에 배포 시 파일 제외 옵션도 없습니다.

### 그동안 하던 방식
매번 수동으로 `plan.md`/`.claude`를 임시 폴더로 옮기고 → 배포하고 → 다시 되돌리는 3단계를 반복 (안전하지만 명령어를 여러 개 손으로 쳐야 해서 시간이 걸림).

### 제안하는 방식
이 3단계를 **스크립트 하나(`deploy.sh`)로 묶어서**, 앞으로는 그 스크립트 한 번 실행으로 끝나게 합니다.
- `.claude/`는 그대로 git에 커밋되고(다른 기기와 동기화 유지), `plan.md`는 지금처럼 프로젝트 폴더 안에서 `code -r`로 계속 열 수 있습니다 — 위치를 옮기지 않습니다.
- 스크립트는: (1) plan.md/.claude를 임시 폴더로 이동 → (2) `wrangler pages deploy .` 실행 → (3) 다시 원위치로 복구, 이 순서를 자동으로 수행합니다.
- 배포 도중 어떤 이유로든 스크립트가 중간에 실패해도, 복구 단계를 항상 실행하도록 만들어서 plan.md/.claude가 임시 폴더에 방치되는 일이 없게 합니다.

## 2. 승인마다 바로 배포하지 않고 모아서 배포

지금까지는 plan.md 승인받을 때마다 바로 커밋 + 배포까지 했는데, 앞으로는:
- **로컬 확인(스크린샷/화면 띄우기)까지만 매번 하고, 실제 배포는 한재님이 "이제 배포해줘" 하실 때 한 번에 모아서** 진행하겠습니다.
- 커밋은 그대로 승인 시점마다 해도 괜찮은지, 아니면 커밋도 같이 모아서 할지 확인 부탁드립니다. (커밋은 로컬/GitHub에만 영향이라 배포처럼 위험하지 않아서, 승인될 때마다 바로 하는 걸 추천합니다.)

---

## 확정된 내용
1. `deploy.sh` 스크립트로 plan.md/.claude 이동→배포→복구를 한 번에 처리. 위치는 그대로 유지.
2. 커밋은 승인 시점마다 바로, 실제 배포(`deploy.sh` 실행)는 한재님이 요청할 때 모아서 진행.

**승인 확인했습니다. 지금 `deploy.sh` 만들고 테스트하겠습니다.**