# 업무 목록 — 구분 · 업무 · 세부업무 3단 구조 + 순서 편집

## 요청 내용
- 업무 목록(업무 테이블)을 **구분 → 업무 → 세부업무** 3단 구조로 정리.
- **각 단계의 순서를 전부 편집(드래그로 재정렬)할 수 있게** 해달라는 요청.

## 지금 이미 있는 것 / 없는 것 (코드 확인 결과)

| 항목 | 현재 상태 |
|---|---|
| 구분(phase)별로 묶어서 보여주기 | ✅ 있음 — `groupTasksByPhase()`로 업무 목록도 이미 구분별 그룹 헤더로 나뉘어 보임 |
| **구분 순서를 직접 드래그로 바꾸기** | ❌ 없음 — 구분 순서는 "그 구분에 속한 첫 번째 업무가 몇 번째냐"로 자동 결정됨. 구분 전체를 통째로 드래그해서 순서를 바꾸는 기능은 없음 |
| 업무 순서를 드래그로 바꾸기 | ✅ 있음 — 지금도 업무 행을 드래그해서 순서 변경 가능 (구분 간 이동도 됨) |
| 세부업무(체크리스트 하위 항목) 자체 | ✅ 있음 — 각 업무를 펼치면 세부업무 추가/체크/이름수정/삭제 가능 (다른 세션이 최근에 추가) |
| **세부업무 순서를 드래그로 바꾸기** | ❌ 없음 — 지금은 추가한 순서 그대로 고정, 위아래로 옮기는 기능 없음 |
| 세부업무 적용 범위 | ⚠️ **현재 "업무 체크리스트" 프로젝트에서만 보임** (`isChecklist` 조건으로 막혀 있음) — "프로젝트 추진일정" 프로젝트에는 세부업무 UI 자체가 안 뜸. DB 컬럼(`subtasks`)은 테이블 타입 구분 없이 공용이라, UI만 열면 추진일정 프로젝트에서도 그대로 쓸 수 있는 구조 |

## 변경 계획

### 1. 구분(phase) 순서 드래그 편집 (신규)
- 구분 그룹 헤더 행에 드래그 핸들 추가 (업무 행 드래그와 같은 방식, 다만 "그 구분에 속한 업무 전체 블록"을 통째로 옮김).
- 드롭하면 그 구분에 속한 모든 업무의 `sort_order`를 통째로 재계산해서 저장 (지금 업무 하나 드래그할 때 전체 `sort_order`를 다시 매기는 방식과 동일한 원리를 그룹 단위로 확장).
- 시각적으로는 지금 업무 행 드래그와 통일된 느낌(같은 커서/하이라이트).

### 2. 세부업무 순서 드래그 편집 (신규)
- 세부업무 각 행(`.subtask-row`)에 작은 드래그 핸들 추가.
- 같은 업무 안에서만 순서 변경 (다른 업무로 옮기는 건 이번 범위 밖).
- 드롭 시 `subtasks` JSON 배열의 순서를 바꿔서 저장 (`updateTaskField(taskId, "subtasks", JSON.stringify(reordered))`).

### 3. 세부업무를 "프로젝트 추진일정"에도 열기
- `buildTaskRowHtml`/`renderTable` 등에서 세부업무 캐럿 버튼·펼침 행을 감싸고 있는 `isChecklist ?` 조건을 제거 — 두 프로젝트 타입 모두에서 세부업무 사용 가능하게.
- DB 컬럼(`subtasks`)은 이미 테이블 타입 구분 없이 공용이라 스키마 변경 불필요.

## 확정된 결정 (한재님 코멘트 반영)
1. 세부업무는 "프로젝트 추진일정"에도 적용 — 체크리스트 전용 제한 제거.
2. 구분 드래그는 **순서 변경만** — 이름/색 편집 기능은 이번 범위 밖 (기존처럼 각 업무 행에서 편집).

## 완료 — 실제 구현 결과
- `buildTaskRowHtml`/`wireDragReorder`에서 `isChecklist ?` 조건 제거 — 세부업무 캐럿·펼침 행·드래그가 두 프로젝트 타입 모두에서 동작.
- 구분(phase) 그룹 헤더에 드래그 핸들(`.group-drag-handle`) 추가, `getPhaseGroupBlockRows()`로 그 구분에 속한 행 전체를 한 덩어리로 옮기는 `wireGroupDragReorder`/`groupDragTracking` 추가.
- 세부업무 각 행에 드래그 핸들(`.subtask-drag-handle`) 추가, 같은 업무 안에서만 순서 변경되는 `subtaskDragTracking` + `persistSubtaskOrder()` 추가.
- **배포 도중 중대한 기존 버그 발견 및 수정**: `subtasks` 컬럼이 실제 Supabase DB에 없었음 (다른 세션이 스키마 파일만 만들고 실행은 안 함) — 그래서 세부업무를 추가해도 화면엔 보이지만 저장은 전혀 안 되고 새로고침하면 사라지는 상태였음. 한재님이 SQL 실행 완료 후 재검증 — 정상 저장/유지 확인.
- Playwright로 실제 검증: 세부업무 추가 → 드래그로 순서 변경 → 새로고침 후 DB에서 순서 유지 확인 / 구분 그룹 드래그로 순서 변경 → 새로고침 후 유지 확인. 안전한 테스트 프로젝트에서 진행 후 원래 순서로 복원, 테스트용 세부업무는 삭제해서 깨끗하게 정리.
- 참고로 발견한 것(이번 범위 밖, 손 안 댐): 아주 짧은 간격(1초 미만)으로 같은 업무의 세부업무를 연달아 추가하면 실시간 동기화 echo가 늦게 도착하면서 방금 추가한 항목이 순간적으로 사라져 보일 수 있는 경쟁 조건이 있음 — 실제 사용 패턴(한 번에 하나씩 천천히 입력)에서는 문제되지 않을 가능성이 높음.