# 작업 계획: 프로젝트 팀별 카테고리 + 체크리스트 계정 로그인

## 요청 내용
1. 프로젝트가 많아질 걸 대비해서, "프로젝트 추진일정" 목록에 **팀별 카테고리**를 만들어주세요.
2. "업무 체크리스트"는 **계정을 만들어서 본인만 접근**할 수 있게 하고, 목록 화면에서 **누구 체크리스트가 있는지도 안 보이게** 해주세요.

두 요청 중 2번(계정 로그인)이 지금까지 작업 중 가장 큰 구조 변경이라 먼저 짚고 넘어가겠습니다.

## 1번: 프로젝트 팀별 카테고리 (비교적 간단)

지금 프로젝트를 만들 때 이미 "부서명"(dept)을 입력받고 있습니다. 새 컬럼을 추가하기보다, **이 dept 값을 팀 카테고리로 재사용**해서 랜딩 페이지의 "프로젝트" 목록을 부서명별로 묶어서(아코디언 또는 그룹 헤더) 보여주는 방식을 제안합니다. 업무 체크리스트 쪽 랜딩은 어차피 2번 때문에 구조가 아예 달라지므로 해당 없음.

- 이대로(부서명 기준 그룹핑) 진행해도 될까요, 아니면 부서명과 별개로 "팀" 카테고리를 새로 만들고 싶으신가요?

dept 값을 팀 카테고리로 재사용해줘

## 2번: 체크리스트 계정 로그인 — 지금 상황과 왜 신중해야 하는지

**지금 이 사이트는 로그인 개념이 아예 없습니다.** DB 접근 권한(RLS)이 "링크만 알면 누구나 열람/수정 가능"으로 완전히 열려있고, 비밀번호는 화면단에서 팀 공용 하나만 거는 정도입니다(수정 권한 여부만 구분). 개인별 계정 + 본인만 접근이라는 건 이 앱에 **처음으로 진짜 인증(로그인) 시스템을 넣는 것**이라, 설계를 잘못하면 나중에 되돌리기 번거롭습니다. 그래서 몇 가지 확인부터 하고 싶습니다.

### 로그인 방식 — 3가지 옵션

- **A안: 이메일 + 비밀번호** (제안) — 회원가입 시 이메일/비밀번호 입력, 이후 로그인. 가장 익숙한 방식이지만 비밀번호 분실 시 재설정 메일 발송 등 부가 기능이 필요합니다.
- B안: 매직링크(이메일로 로그인 링크만 받기) — 비밀번호 관리가 아예 필요 없어서 구현이 더 간단하지만, 매번 이메일을 열어서 링크를 눌러야 로그인됩니다.
- C안: 구글 로그인 연동 — 회사 구글 계정으로 바로 로그인. 별도 비밀번호 관리가 없어 편하지만, Google Cloud 설정이 하나 더 필요합니다(OAuth 앱 등록).

구글 로그인 연동하자

### 확인이 필요한 점

1. **어느 로그인 방식으로 할까요?** (A/B/C 중, 또는 다른 생각이 있으신지)
2. 체크리스트는 **한 사람이 여러 개 만들 수 있나요, 아니면 계정당 1개**로 제한할까요? ("김태경 업무 체크리스트"처럼 이름이 붙어있는 걸 보면 여러 개도 가능하게 하는 게 자연스러워 보입니다만 확인차 여쭤봅니다)

체크리스트는 한 사람당 하나로 제한해줘.

3. **기존 "김태경 업무 체크리스트"는 어떻게 할까요?** 실제로 김태경님이 계정을 만들어서 그 계정 소유로 넘겨야 하는데, 그 전까지는 누구 소유도 아닌 상태가 됩니다. 일단 그대로 두고 나중에 김태경님이 가입하면 넘겨드리는 방식으로 괜찮을까요?

내 구글 계정으로 소유권 이전해줘

4. **프로젝트(추진일정)는 지금처럼 로그인 없이 링크 공유 방식 그대로 유지**하는 게 맞을까요? (체크리스트만 계정 기반으로 바뀌고, 프로젝트는 그대로)

엉 그대로 유지

## 로그인 도입 시 대략적인 구조 (참고용)
- Supabase Auth 사용 (이미 Supabase를 쓰고 있어서 같은 프로젝트 안에서 바로 활성화 가능, 추가 비용 없음)
- `projects` 테이블에 `owner_user_id` 컬럼 추가 → 체크리스트 생성 시 로그인한 사용자의 id로 채워짐
- DB 접근 권한(RLS)을 프로젝트 타입별로 분리: 추진일정(`timeline`)은 지금처럼 전체 공개, 체크리스트(`checklist`)는 `owner_user_id`가 본인일 때만 열람/수정 가능
- 랜딩 페이지: 로그인 안 한 상태면 "업무 체크리스트" 영역에 로그인/회원가입 버튼만 보이고, 로그인하면 본인 체크리스트만 조회해서 보여줌 (다른 사람 체크리스트는 애초에 서버에 요청도 안 함 → 목록에 안 보임)

---
## 확정
1. 프로젝트: dept(부서명) 기준으로 그룹핑
2. 체크리스트: 구글 로그인, 계정당 1개 제한
3. 김태경 체크리스트 → 한재님 구글 계정으로 소유권 이전
4. 프로젝트(추진일정)는 로그인 없이 그대로 유지

## 진행 순서
1. (지금) 부서명 기준 그룹핑 — 바로 구현/테스트/배포
2. 구글 로그인 코드 + DB 구조 준비
3. **한재님이 직접 해주셔야 하는 부분**: Google Cloud Console에서 OAuth 클라이언트 생성 → Supabase 대시보드 Authentication 설정에 등록 (단계별로 안내드리겠습니다)
4. 설정 완료되면 한재님이 사이트에서 구글 로그인 한 번 → 그 계정으로 김태경 체크리스트 소유권 이전
