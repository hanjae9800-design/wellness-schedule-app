# 간트차트·업무 목록 통합 + 탭 폴더 스타일 UI (7차)

## 요청 내용
- "간트차트와 업무목록을 나누지말고 마치 올린 이미지같은 UI를 구현해줬으면 좋겠어"
- 첨부 이미지: 크래프트지 느낌의 서류 파일철 — 초록/크림/하늘색/테라코타 색 탭 4개("WELCOME", "VIP", "ISLANDERS", "BATCH 2")가 계단식으로 겹쳐 튀어나온 인덱스 카드 폴더 디자인.

## 확인하고 싶은 부분 (해석이 갈릴 수 있어서 확정 짓고 진행하려고 합니다)

지금 앱은 좌상단 `☰` 메뉴로 **전체업무 / 간트차트 / 업무 목록 / 담당자별 업무** 4개 화면을 전환하는 구조입니다 (전체업무를 고르면 간트차트+업무 목록이 같이 보이고, 간트차트/업무 목록을 각각 고르면 하나만 보임). "나누지말고"라고 하신 게 정확히 뭘 가리키는지에 따라 구현이 많이 달라집니다.

1. **(A) 화면 전환 자체를 없애기**: 지금처럼 "간트차트만 보기" / "업무 목록만 보기"로 따로 전환하는 기능을 없애고, **항상 간트차트 + 업무 목록이 한 페이지에 같이 보이게** 만든다. (지금의 "전체업무" 모드를 기본이자 유일한 모드로 만드는 것에 가까움)
2. **(B) 두 패널을 물리적으로 하나의 UI 오브젝트로 묶기**: 지금처럼 각자 독립된 카드(박스)로 위아래에 떨어져 있는 게 아니라, 첨부 이미지의 파일철처럼 **하나의 폴더 안에 간트차트/업무 목록이 탭으로 붙어있는 모양**으로 만든다 (예: 폴더 뒤쪽에 "간트차트", "업무 목록" 탭이 계단식으로 붙어 있고, 탭을 누르면 그 안의 내용이 앞으로 오는 식).
3. **(C) 이미지의 탭 디자인을 다른 곳에 적용**: 지금 간트차트/업무 목록 위에 있는 "구분"(기획·섭외·콘텐츠·시스템·홍보) 필터가 동글동글한 배지 모양인데, 이걸 이미지처럼 **계단식으로 겹친 탭 모양**으로 바꾸는 것.

제가 보기엔 **1번(화면 전환 없애고 항상 같이 보이게) + 3번(구분 필터를 탭 폴더 모양으로)**을 합친 게 가장 자연스러운 해석 같습니다 — 간트차트/업무 목록을 나누지 말라는 요청과, 폴더 이미지를 UI로 구현해달라는 요청을 각각 그대로 반영하는 조합이라서요. 2번(둘을 아예 하나의 물리적 탭 폴더 오브젝트로 만들기)은 시각적으로는 이미지에 제일 가깝지만, 그러면 결국 "간트차트 탭 / 업무 목록 탭"으로 다시 나뉘게 돼서 "나누지 말라"는 요청과는 좀 어긋나는 것 같습니다.

**어떤 방향이 맞을까요?** (1+3 조합으로 진행해도 될지, 아니면 다른 그림을 그리고 계신지 알려주시면 그에 맞게 구체적인 색상·탭 배치까지 잡아서 진행하겠습니다.)

1+3번 조합이 맞고 이미지처럼 사이트에서 보이는 맨 처음 이미지가 폴더들의 탭만 보여지면서 그 탭을 클릭하면 클릭한 탭이 펼쳐지는 물리적 오브젝트도 만들어줘

## 참고: 지금 구조
- `☰` 메뉴(`#viewNavPanel`): 목록 / 전체업무 / 간트차트 / 업무 목록 / 담당자별 업무
- `applyViewMode()` (app.js): `viewMode`에 따라 `#ganttViewGroup`/`#tableViewGroup`의 `hidden`을 토글
- "구분" 필터: 간트차트 상단 `.legend`(색색의 둥근 배지, `기획/섭외/콘텐츠/시스템/홍보`), 업무 목록에서는 각 그룹 헤더 행(`.phase-group-toggle`)이 이미 진한 배경색 막대 형태

## 확정된 방향 (한재님 코멘트 반영)
1+3 조합 확정, 추가로: **첫 화면엔 탭만 보이고(폴더가 닫힌 상태), 탭을 클릭하면 그 탭이 펼쳐지는** 물리적 오브젝트로 구현.

## 구체적 구현안 (제안 — 확인 후 진행)

**전체 그림**: 지금의 `#ganttViewGroup`(간트차트) / `#tableViewGroup`(업무 목록)을 완전히 걷어내고, 그 자리에 **구분(phase)별 폴더 탭 컴포넌트** 하나만 둡니다.
- 페이지를 열면 **탭만 계단식으로 겹쳐서 보임** (기획/섭외/콘텐츠/시스템/홍보 등, 구분마다 지정된 색으로 — 지금 쓰는 `--p1~--p10` 팔레트 그대로 사용). 첨부 이미지처럼 각 탭이 조금씩 밀려서 겹쳐 보이는 계단식 배치.
- 탭을 클릭하면 그 탭이 아래로 펼쳐지면서 그 구분에 속한 업무들이 나타남 (아코디언처럼 한 번에 하나만 펼쳐지는 방식을 기본으로 하려고 합니다 — 실제 서류 파일철에서 한 칸씩 당겨보는 느낌과 가장 가까워서. 여러 개를 동시에 펼쳐두고 싶으시면 말씀해주세요).

서류 파일철과 같은 컨셉으로 해주면 돼

- **펼쳐진 안쪽 내용물**: 여기서 선택지가 갈립니다 —
  - **(추천) 지금 있는 두 가지(간트 막대가 있는 행 / 담당자·시작일·종료일 등을 편집하는 업무 목록 행)를 새로 하나로 합치지 않고, 펼친 탭 안에 그대로 순서대로 둡니다** — 즉 탭을 펼치면 그 구분의 미니 간트차트(막대)가 먼저 보이고 바로 아래에 그 구분의 업무 목록(담당자 지정, 세부업무 체크, 드래그 순서변경, 선행업무 지정 등 지금 되는 모든 편집 기능 그대로)이 이어서 보이는 구조. **"화면을 나누지 않는다"는 요청은 "간트차트만 보는 화면 / 업무 목록만 보는 화면으로 전환하지 않는다"는 뜻으로 해석해서, 탭을 열면 두 정보가 항상 함께 보이도록 하는 것으로 충족**시키려고 합니다.
  - (대안) 간트 막대 + 업무명 + 담당자 + 상태를 아예 한 줄짜리 새로운 행 디자인으로 처음부터 다시 만들기 — 더 "안 나뉜" 느낌이지만, 지금 업무 목록에 있는 세부 기능들(드래그 재정렬, 세부업무 체크리스트, 선행업무 지정, 담당자 select, 시작일/종료일 편집, 컬럼 너비 조절 등)을 전부 새 행 디자인 안에 다시 넣어야 해서 작업량과 회귀 위험이 훨씬 큽니다. 이번 세션에서 여러 번에 걸쳐 다듬어온 기능들이라 조심스럽습니다.
  - → **추천안(기존 두 위젯을 유지한 채 탭 안에 같이 넣는 방식)으로 진행해도 될까요?** 이 방식이 시각적 효과(폴더+탭)와 "안 나뉜다"는 느낌을 충분히 주면서, 지금까지 쌓아온 편집 기능들을 안 건드리는 가장 안전한 길이라고 판단했습니다.
- "담당자별 업무" 보기는 이번 변경과 별개로 그대로 유지 (뷰 메뉴에서 그대로 선택 가능하게 둘지, 이것도 폴더 안에 통합할지는 이번 요청 범위 밖이라 우선 그대로 둡니다).
- `☰` 뷰 전환 메뉴의 "간트차트" / "업무 목록"을 따로 고르는 옵션은 없어지고(항상 같이 나오니까), "전체업무"라는 이름도 이제 의미가 없어져서 정리가 필요합니다 — 메뉴에는 "목록 / 담당자별 업무" 정도만 남기려고 합니다.
- 모바일은 이번에도 손 안 대는 걸로 하고(그동안의 규칙), 데스크톱 화면(`.board.desktop-only`)에만 적용합니다.

## 작업 범위 밖
- 담당자별 업무 보기는 이번 변경에 안 넣음.
- 모바일 화면은 이번에 안 건드림.
- 업무 행 자체의 디자인/기능은 안 바꿈 (탭으로 감싸는 틀만 새로 만듦).

2번으로 진행

## 미리보기 단계 (Artifact로 먼저 검증 중)
- 큰 재작업이라 실제 앱을 바로 고치지 않고, Artifact로 폴더 탭 미리보기를 만들어서 여러 차례 디자인 피드백을 받는 중입니다: https://claude.ai/code/artifact/0dd5737f-7efc-47bc-a069-4793ff0a3590
- 지금까지 반영된 피드백:
  1. 양옆 여백 줄이고 화면 폭 넓게
  2. "실제 파일 모양 같지 않다" → 종이 표지(제본선) + 탭이 종이 상단에서 튀어나온 실제 서류철 형태로 재설계 (v2)
  3. 나중 탭(순서상 뒤)이 앞으로 오도록 z-index 반대로, 탭 사이 간격도 여유있게
  4. **업무 안에 세부업무 행 추가 — 구분(탭) → 업무(행) → 세부업무(체크리스트) 3뎁스** 완성 (캐럿 버튼으로 펼침/접힘)
  5. 캐럿 버튼 크기 키움 (18px → 26px)
  6. **왼쪽에 사이드바 추가**: "☀️ 오늘 할 일" / "🎯 주간 목표" / "📌 체크할 일" 3개 카드

## 사이드바 위젯 실사용 여부 확정
- "오늘 할 일"은 실제 업무의 시작일/종료일로 자동 계산 가능 (기존 `taskFilters.today` 로직과 동일한 방식).
- "주간 목표"·"체크할 일"은 지금 앱 데이터에 없는 새 개념이라 물어봤고 → **"두 위젯 실제로 쓸거야"** 확정. 새 저장 공간(Supabase 테이블)이 필요합니다.

### 제안하는 데이터 모델 (확인 필요)
- 이 앱은 로그인 없이 링크로 공동 편집하는 구조(체크리스트 타입만 예외)라, 아래 두 위젯도 **업무처럼 프로젝트 단위로 저장**해서 그 프로젝트 링크를 가진 모두에게 실시간으로 같이 보이게 하려고 합니다 (프로젝트 안 다른 데이터들과 동일한 방식).
  - `weekly_goals` 테이블: `id, project_id, name, pct(진행률 0~100), sort_order, created_at`
  - `check_items` 테이블: `id, project_id, name, done, sort_order, created_at`
- "주간"이라는 이름이 붙어있는데, 매주 자동으로 초기화되는 기능까지 필요하신가요, 아니면 그냥 직접 추가·삭제·진행률 조정하는 목록이면 될까요? (자동 초기화는 "몇 요일에 리셋할지" 등 별도 설계가 필요해서 확인하고 싶습니다.)
- 이 데이터 모델과 "주간 목표 자동 초기화 여부"만 확인해주시면, 실제 앱(Supabase 테이블 추가 + app.js/index.html/style.css)에 전체를 옮기는 작업(폴더 탭 + 세부업무 3뎁스 + 사이드바 3위젯) 시작하겠습니다.

## 확정: 주간 목표는 수동 관리, 자동 초기화 없음
"주간 목표는 직접 추가,삭제,진행률 조정도 가능하게 해줘. 매주 자동으로 초기화하지마 그대로 둬" → `weekly_goals` 테이블 그대로(pct 직접 조정, 자동 리셋 로직 없음)로 확정.

## 완료 — 실제 앱 구현 결과

**DB**: `sidebar-widgets-schema.sql` 작성해서 전달드림(직접 실행 필요 — anon 키로는 테이블 생성 권한이 없어서). 실행 전에는 두 위젯이 "아직 없습니다" 빈 상태로만 보이고 에러 없이 동작(추가 시도하면 SQL 실행 안내 문구가 뜸).

**폴더 탭 (구분 → 업무 → 세부업무)**:
- `#ganttViewGroup`/`#tableViewGroup`(기존 간트·표 코드)은 **지우지 않고 그대로 둠** — 모바일 "타임라인 차트 보기" 오버레이가 간트 렌더링 함수를 그대로 쓰고, "담당자별 업무" 보기가 기존 표 렌더링을 그대로 쓰기 때문. 데스크톱 기본 화면에서만 새 `#folderViewGroup`으로 교체.
- 구분마다 계단식 탭(`.sheet-tab`), 클릭하면 그 구분만 펼쳐짐(아코디언, 서류철 컨셉 그대로).
- 업무 행 = 이름(편집)·담당자(select)·현황(select)·일정 막대·"세부내용"·삭제 버튼 한 줄. 업무 전체 날짜 범위 기준으로 막대 위치를 %로 계산(전용 가로 스크롤바 없이 항상 한눈에 보임 — 미리보기 때 확정한 방식).
- 캐럿을 누르면 세부업무 체크리스트가, "세부내용"을 누르면 시작일/종료일/선행업무/비고가 펼쳐짐(둘 다 동시에 열 수 있음). 드래그 재정렬(업무·세부업무 모두)도 그대로 구현.
- 구분 추가("+새 구분 추가")·이름 바꾸기(✎, `prompt()`) 지원. 색상 변경 UI는 이번엔 안 만듦(필요하시면 말씀해주세요).
- 같은 구분 안에서만 순서 변경 가능 — 아코디언 특성상 다른 구분으로 드래그하는 건 원래도 안 보여서 불가능.

**사이드바**: 오늘 할 일(시작일~종료일에 오늘이 걸친 업무를 자동으로 모음, 체크하면 완료 처리), 주간 목표(이름 수정·진행률 바 클릭으로 % 조정·삭제), 체크할 일(이름 수정·체크·삭제) — 전부 실시간 동기화(다른 사람이 편집해도 즉시 반영).

**버그 하나 발견·수정**: 처음 배포했을 때 탭이 안 열리는 문제가 있었음 — 브라우저가 버튼을 클릭하면 자동으로 포커스를 주는데, "입력 중이면 재렌더링 미루기" 가드가 이걸 "텍스트 입력 중"으로 착각해서 재렌더링 자체를 막고 있었음. input/textarea/select에만 반응하도록 가드를 좁혀서 해결.

**검증**: 안전한 테스트 프로젝트에서 Playwright로 직접 조작 — 탭 열기/닫기, 세부업무 추가·체크·삭제, 세부내용(날짜·선행업무 지정/해제·비고), 같은 구분 안 드래그 재정렬(순서 바꾸고 DB 확인 후 원상복구), 업무 추가/삭제, 구분 추가/삭제, 담당자별 업무 보기, 라이트/다크 모드, 모바일(카드 목록 + 타임라인 오버레이) 전부 정상 확인. 콘솔 에러 없음(주간목표/체크할일 테이블 없다는 예상된 에러만 있음). 테스트로 만든 데이터는 전부 정리해서 원래 16개 업무·5개 구분 상태로 복원.

---

# 사이드바(오늘할일/주간목표/체크할일)를 화면 좌측 끝에 고정 (8차)

## 요청
"오늘할일, 주간목표, 체크할 일을 맨 왼쪽에 사이드 바로 만들어줘 좌측 끝에 일자로 붙여줘"

## 지금 상태
지금도 이 3개 위젯은 왼쪽에 있긴 하지만, **화면의 진짜 왼쪽 끝이 아니라** 가운데 정렬된 본문 컨테이너(`.app`, `max-width:1280px; margin:0 auto`) 안에 들어있어서, 화면이 넓을수록 왼쪽에 빈 여백이 그만큼 남고 위젯은 안쪽으로 밀려 보입니다. 요청하신 "좌측 끝에 일자로 붙여줘"는 이 여백 없이 **브라우저 화면 왼쪽 끝에 딱 붙은 세로 막대(rail)**로 만들어달라는 뜻으로 이해했습니다.

## 구현 방향
- 사이드바(`.side`)를 가운데 정렬 컨테이너(`.app`)의 폭 제약에서 빼내서, 화면 왼쪽 끝(x=0)부터 시작하는 고정 폭(272px) 세로 영역으로 만듭니다. (CSS의 "풀블리드" 기법 사용 — 상단바 아래로, 화면을 스크롤해도 같이 스크롤되되 일정 지점부턴 붙어있는 지금의 sticky 동작은 그대로 유지)
- 시각적으로 "막대"처럼 보이도록 옅은 배경색 + 오른쪽 경계선을 넣어서, 본문과 경계가 분명한 진짜 사이드바 느낌을 주려고 합니다 (지금은 카드 3개가 그냥 떠 있는 느낌).
- 본문(업무 폴더 탭 영역)은 사이드바 폭만큼 오른쪽으로 밀려서, 겹치지 않고 그대로 다 보이게 합니다.
- 화면이 좁아지는 구간(태블릿 정도, 지금 기준 1180px 이하)에서는 지금처럼 사이드바가 위쪽에 가로로 눕는 방식(붙여진 rail이 아니라 일반 카드형)으로 자동 전환 — 화면이 좁을 때 272px를 왼쪽에 고정으로 뺏기면 본문이 너무 좁아지기 때문에, 이 예외는 유지하려고 합니다.
- 모바일(880px 이하)은 이번에도 손 안 댐 — 원래도 이 사이드바 자체가 데스크톱 전용(`.board.desktop-only`) 이라 모바일엔 안 보입니다.
- "담당자별 업무" 화면으로 전환하면 지금처럼 사이드바도 같이 사라집니다 (이번 요청 범위 밖 — 그대로 둠).

## 확정 (한재님 답변 반영)
"확실한 막대 느낌, 사이드바 자체를 따로 스크롤" → 아래 2가지 확정:
1. 사이드바에 옅은 배경색 + 오른쪽 경계선을 넣어서 본문과 확실히 구분되는 막대 형태로 만듦.
2. 사이드바는 화면 세로로 꽉 채우고(상단바 아래부터 화면 끝까지), 내용이 길어지면 페이지 전체가 아니라 **사이드바 자체만 따로 스크롤**(`overflow-y:auto`) — 즉 본문(업무 폴더 탭)을 아무리 내려도 사이드바는 화면에 계속 고정된 채 그 안에서만 스크롤됨.

## 구현 메모 (진행 시 적용할 구체안)
- `.side`: `position: sticky` 대신 뷰포트 기준으로 화면 왼쪽 끝(x=0)에 딱 붙게, 상단바(56px) 바로 아래부터 화면 맨 아래까지 세로로 꽉 채움. `overflow-y: auto`로 내부 콘텐츠만 스크롤. 배경색(`var(--surface)`) + 오른쪽 경계선(`var(--border)`) 추가.
- `.folder-layout`이 가운데 정렬 컨테이너(`.app`, max-width:1280px)의 폭 제약 밖으로 벗어나도록 처리(풀블리드 기법)해서, 사이드바가 실제로 화면 왼쪽 끝에서 시작하도록 함. 본문(`.folder-main`)은 사이드바 폭만큼 오른쪽으로 밀어서 안 겹치게 함.
- 1180px 이하(태블릿 폭)에서는 지금처럼 사이드바가 위쪽에 가로로 눕는 카드형으로 자동 전환(고정 막대 X) — 본문 공간 확보를 위해 유지.
- 모바일(880px 이하)은 원래도 데스크톱 전용이라 영향 없음.

이대로 진행해도 될까요?

## 완료
그대로 구현·배포함. 추가로 발견해서 같이 고친 것: 사이드바가 고정되면서 그 위 공통 영역(통계 바 "완료/진행중/지연" 칩, "담당자별 현황" 버튼)이 사이드바에 가려 잘려 보이는 문제가 있었음 — `applyViewMode()`에서 `#app`에 `has-side-rail` 클래스를 붙여서 그 영역들도 같이 밀어주도록 처리. Playwright로 넓은 화면/1300px/1180px 이하 태블릿 폭/다크모드/사이드바 자체 스크롤(페이지는 안 움직이고 사이드바만 스크롤됨 확인)/담당자별 업무 전환 시 사이드바·마진 깔끔히 사라지는지/모바일 전부 확인 완료. 커밋 `a5ae2d2`.

---

# 사이드바를 "MY FILE" 종이 파일철 스타일로 재단장 (9차)

## 요청
참고 이미지("MY FILE" 대시보드 목업) 첨부하며 "이런 느낌으로 만들고 싶어" → **wellness-schedule-app의 지금 사이드바(오늘 할 일/주간 목표/체크할 일)를 이 스타일로 재단장**하는 것으로 확인함 (참고 이미지 속 "프로젝트/캘린더/설정" 등 별도 페이지 메뉴는 이 앱에 해당하는 개념이 없어서 그대로 옮기지 않음 — 스타일만 가져옴).

## 참고 이미지에서 읽히는 디자인 요소
- 전체가 **크림/탠 톤의 종이·파일철 질감** (지금 앱의 바우하우스 크림 팔레트와 톤은 비슷하지만, 이미지는 종이 재질감·바인더 클립 같은 아날로그 디테일이 강함)
- 왼쪽 끝에 **바인더 클립**(집게) 장식이 페이지 가장자리에 꽂혀 있는 디테일
- 콘텐츠가 카드로 뚝뚝 분리되어 있지 않고, **하나의 연속된 종이 패널** 위에 섹션들이 얇은 구분선으로만 나뉨
- "오늘의 할일": 체크박스 + 항목명 + (시간) + "+ 할일 추가" + "전체보기 →"
- "주간 목표": 큰 숫자로 "3 / 5" 완료 카운트 + 전체 진행률 바(60%) + 번호 매겨진 목표 목록, 각 항목 오른쪽에 상태 배지(완료/진행 중/대기)

## 지금 사이드바와 비교했을 때 그대로 못 옮기는 부분
- **시간 표시**(10:00, 14:00 등): 지금 "오늘 할 일"은 업무의 시작일~종료일 기준으로만 모으고 있어서 시간 단위 데이터가 없음 → 시간 대신 지금처럼 상태 배지(진행중/지연 등)를 쓰는 게 맞다고 생각합니다.
- **주간 목표 "3/5" 전체 진행률 바**: 지금은 목표마다 각자 %만 있고, 목표 전체를 합산한 "완료 개수/전체 개수" 같은 집계는 없음 → 이건 새로 계산해서 추가하면 되는 부분입니다 (예: pct가 100인 목표 수 / 전체 목표 수).
- 이미지 왼쪽의 "MY FILE" + 대시보드/오늘의 할일/주간 계획/성과 리포트/프로젝트/캘린더/설정 내비게이션은 안 가져옴(해당 없음).

## 확정 (한재님 답변)
"1. B, 2번은 그냥 하지말고 진행" →
1. **(B) 깊은 재구성으로 확정**: 카드 3개 구분 없이 하나의 이어진 종이 패널로 재구성 + 바인더 클립 장식 + 주간 목표 전체 진행률("N/5" + 집계 % 바) 반영.
2. **Artifact 미리보기 단계는 생략** — 바로 실제 앱(`index.html`/`app.js`/`style.css`) 코드로 진행.

## 구현 메모 (진행하면서 적용)
- `.side` 전체를 카드 3개(`.side-card` × 3) 대신 **하나의 종이 패널**로 바꾸고, 그 안에서 "오늘의 할일" / "주간 목표" / "체크할 일" 섹션을 얇은 구분선(border-top 등)으로만 나눔.
- 패널 배경은 종이 질감(은은한 노이즈/텍스처 그라디언트 — 지금 `.sheet-page::before`에 쓰는 것과 비슷한 미세 줄무늬 텍스처를 재사용하거나 참고)을 넣어서 밋밋한 단색과 구분되게 함.
- 사이드바 왼쪽 가장자리에 **바인더 클립(집게) 장식**을 CSS로 하나 추가 (실제 클립 모양을 흉내낸 작은 장식 요소, 상단에서 조금 내려온 위치에 고정).
- "주간 목표" 섹션 상단에 **"N / 전체" 완료 카운트 + 그 아래 전체 진행률 바**를 새로 추가 — `pct === 100`인 목표 수 / 전체 목표 수로 계산. 기존에 목표마다 있던 개별 진행률 바/조정 기능(클릭해서 % 조정)은 그대로 유지.
- "오늘의 할일" 항목엔 시간 대신 지금처럼 상태 배지(진행중/지연 등) 유지, 참고 이미지의 "+ 할일 추가"/"전체보기 →" 같은 문구·번호 매김 스타일은 우리 쪽 표현에 맞게 톤만 가져옴.
- 전체 색감은 지금 앱의 라이트/다크 테마 변수(`--surface`, `--surface-alt`, `--border` 등)를 그대로 활용해서, 라이트/다크 모드 둘 다 자연스럽게 적용되도록 함 (이미지 자체는 라이트 모드 기준이라, 다크 모드 배색은 지금 앱 톤에 맞게 알아서 조정).
