# 간트차트 "오늘" 표시를 세로선 → 해당 날짜 열 테두리 강조로 변경

## 요청 내용
- 지금은 오늘 날짜를 세로 주황색 선(굵은 4px 라인)으로 표시 중.
- 이걸 "선"이 아니라, **오늘에 해당하는 날짜 열(하루치 너비) 자체를 주황색 테두리로 감싸는 방식**으로 바꿔달라는 요청.
- 목적: 오늘이 언제인지는 여전히 한눈에 알아볼 수 있으면서, 그 날짜 칸 위에 겹쳐 있는 업무 막대/글자 등 내용을 선처럼 가리지 않게.

## 현재 상태
- `buildGanttHtml()`([app.js](app.js))에서 `.gantt-today-line` 하나(`left`, `width:4px`, 배경색 꽉 채움)를 그리고, 그 안에 `.gantt-today-label`(오늘 M/D 텍스트)을 얹는 구조.
- 방금 전 작업에서 이 선이 업무 목록(왼쪽 고정 라벨) 영역으로 삐져나오는 문제를 고치기 위해 `.gantt-label-curtain`(라벨 폭만큼 통짜 배경, 스크롤 동기화)을 추가해둔 상태 — 이 보호 장치는 "오늘 표시가 선이든 테두리든" 상관없이 라벨 영역을 가려주는 범용 장치라 그대로 재사용 가능.

## 변경 계획

### 1. `.gantt-today-line` → `.gantt-today-col` (박스 형태로 교체)
- 위치/크기: `left`는 지금과 동일하게 계산(`ganttLabelWidth + todayOffsetDays*daypx`), **`width`를 하루 칸 너비(`daypx`)로** 지정 (지금은 4px 고정폭).
- 스타일: 배경은 투명 또는 아주 옅은 틴트(예: `background: color-mix(in srgb, var(--today) 12%, transparent)` 같은 은은한 색), **테두리만 `border: 2px solid var(--today)`**로 강조 — 안쪽 내용(업무 막대, 글자)이 그대로 비쳐 보이게.
- 세로 범위는 지금처럼 헤더 아래(`top: var(--gantt-header-h)`)부터 맨 아래(`bottom:0`)까지 유지.
- "오늘 (M/D)" 라벨은 이 박스 위쪽에 그대로 유지(위치만 박스 폭에 맞게 살짝 조정 가능).

### 2. 라벨 영역 삐져나옴 방지 장치는 그대로 유지
- `.gantt-label-curtain`은 특정 모양(선/박스)에 의존하지 않는 범용 가림막이라 수정 없이 그대로 today-col에도 적용됨 — 별도 작업 불필요.

### 3. 배포/검증
- 배포 후 실제 화면에서: 오늘 날짜 칸이 테두리로 표시되는지, 그 칸 위에 업무 막대가 있을 때 막대가 가려지지 않고 보이는지, 라이트/다크 모드 둘 다 스크롤하면서 라벨 영역으로 안 삐져나오는지 확인.

## 확인이 필요한 부분 (해결됨)
- 색/틴트 여부는 미리보기 아티팩트로 두 차례 조정하며 확정: **민트색(`--today` 토큰을 주황→민트로 변경) 테두리 + 옅은 틴트(13%)**, 날짜 텍스트 라벨 없음, 월/날짜 헤더 행까지 이어짐.

## 완료 — 실제 구현 결과
- `--today` 색 토큰을 라이트 `#0d9488`, 다크 `#2dd4bf`로 변경 (기존 주황 → 민트).
- `.gantt-today-line`+`.gantt-today-label`을 `.gantt-today-col` 하나로 교체 — 좌우 테두리 2px + 옅은 배경, `top:0`부터(헤더 포함) `bottom:0`까지.
- 날짜 하드코딩 헤더 높이 보정(`syncGanttHeaderHeight`/`--gantt-header-h`)은 더 이상 필요 없어져서 제거.
- **오늘 표시 켜기/끄기 버튼**을 간트차트 바로 위 툴바 우측(구분 범례 옆)에 추가 (`#todayToggleBtn`) — `localStorage`에 상태 저장, 클릭 시 즉시 반영.
- 배포 후 실측 검증: 강조 칸 너비(20px)가 날짜 눈금 정확히 한 칸과 일치, 오늘(2026-08-30 "30")에 정확히 위치. 토글 켜기/끄기 둘 다 정상 동작 확인. 라벨 영역으로 삐져나오는 문제도 기존 `.gantt-label-curtain` 장치가 그대로 막아줌.
