# 작업 계획: "업무 체크리스트" 모드 추가

## 요청 내용
사이트에 모드를 2개로 나눕니다:
- **프로젝트 추진일정** (지금 있는 것 그대로)
- **업무 체크리스트** (담당자 개인이 본인 업무를 데일리로 체크하고 계획하는 용도)

사이트 링크를 아는 사람이 랜딩 페이지에서 "새 프로젝트 만들기" / "내 업무 체크리스트 만들기" 중 선택할 수 있게 하고, 이미 만들어져 있는 **"김태경 업무 체크리스트"** 프로젝트를 이 모드의 테스트 모델(기준)로 삼습니다.

## 조사한 내용 — "김태경 업무 체크리스트"는 실제로 어떻게 쓰이고 있나
Supabase에서 실제 데이터를 확인해봤습니다:
- 업무 4개가 등록되어 있고, **구분(phase_name)에 상위 사업명**(예: "서울웰니스관광100선", "서울웰니스트래블위크", "글로벌 웰니스 컨퍼런스 2026")을 넣어서 "이 업무가 어떤 큰 사업 소속인지"로 쓰고 있습니다.
- 시작일/종료일이 대부분 **오늘 하루**(2026-08-25)로 되어 있고, 비고 칸에 통화 상대·전화번호·구체적인 진행 메모를 길게 적어두는 방식입니다.
- 현황(예정/진행중/완료)은 지금 앱의 3단계 상태를 그대로 쓰고 있습니다.
- 담당자는 본인(김태경) 한 명 고정.

즉 **이미 지금 앱 구조(구분/업무명/현황/시작일·종료일/비고)를 그대로 "일일 업무 체크리스트"처럼 쓰고 계셨던 것**으로 보입니다. 그래서 완전히 새로운 데이터 구조를 만들기보다, 지금 구조를 재사용하면서 **화면 구성만 체크리스트에 맞게 간소화**하는 방향을 제안드립니다.

## 제안하는 설계

### 1. 프로젝트에 "타입" 구분 추가
`projects` 테이블에 `type` 컬럼 추가 (`timeline` / `checklist`, 기본값 `timeline` — 기존 프로젝트들은 전부 그대로 `timeline`으로 유지되어 영향 없음).

### 2. 랜딩 페이지
"새로 만들기" 박스를 지금 하나에서, **"새 프로젝트 만들기" / "내 업무 체크리스트 만들기"** 두 개로 분리. 후자를 선택하면 `type: "checklist"`로 프로젝트가 생성됩니다.

### 3. 체크리스트 모드 화면 (제안 — A안)
같은 업무 목록 테이블/모바일 카드 구조를 그대로 쓰되:
- **간트차트, 담당자별 업무 보기, 화면 전환 메뉴는 숨김** (혼자 쓰는 화면이라 필요 없음)
- 업무 목록 위에 **"오늘" 필터**를 기본으로 켜서, 시작일~종료일에 오늘이 걸치는 업무만 먼저 보여주고, 클릭하면 전체 보기로 전환 가능
- 나머지(구분별 그룹핑, 현황 3단계, 세부내용 펼침, 비고 등)는 지금 그대로 사용

담당자별 업무 보기, 화면 전환 메뉴는 숨겨주고 간트차트는 유지해줘 차트 보면서 어떤 업무를 언제까지 할지 볼 수 있도록

### 4. "김태경 업무 체크리스트"를 기준 모델로
이 프로젝트의 `type`을 `checklist`로 바꿔서, 위 화면이 적용되는 실제 첫 사례로 만듭니다.

## 확인이 필요한 점

1. **체크리스트 화면 구성이 A안(위 3번)으로 괜찮으신가요?** 아니면 생각하신 모습이 더 구체적으로 다른가요? (예: 체크박스로 완료 표시하는 더 단순한 형태, 요일별로 나눠보기 등)
2. 랜딩 페이지에 프로젝트 카드가 지금은 구분 없이 하나의 목록인데, **체크리스트와 일반 프로젝트를 섞어서 보여줄지 / 구분해서 두 목록으로 나눌지**?

구분해서 두 목록으로 나누자

3. 기존 프로젝트들(전체)은 전부 지금처럼 "프로젝트 추진일정"으로 유지되는 게 맞을까요? (제 이해로는 맞을 것 같습니다만 확인 차 여쭤봅니다)

맞어

---
## 확정 — 구현 완료
- 간트차트는 숨기지 않고 유지 (담당자별 업무 보기 / 화면 전환 메뉴만 숨김)
- 랜딩 페이지 프로젝트 카드를 "프로젝트" / "업무 체크리스트" 두 목록으로 분리
- 기존 프로젝트는 전부 `timeline` 유지, "김태경 업무 체크리스트"만 `checklist`로 전환
- `type-schema.sql` 마이그레이션 실행 완료, 로컬 테스트 통과 (오늘 필터, 간트 표시, 랜딩 분리 모두 확인)
